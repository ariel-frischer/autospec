tasks:
    branch: "128-persist-feature-directory"
    created: "2026-05-14"
    spec_path: "specs/128-persist-feature-directory/spec.yaml"
    plan_path: "specs/128-persist-feature-directory/plan.yaml"
summary:
    total_tasks: 24
    total_phases: 6
    parallel_opportunities: 8
    estimated_complexity: "medium"
phases:
    - number: 1
      title: "Setup"
      purpose: "Establish the active feature resolver surface without changing workflow behavior."
      tasks:
        - id: "T001"
          title: "Add active feature resolver API skeleton in internal/spec/active_feature.go"
          status: "Completed"
          type: "setup"
          parallel: false
          story_id: null
          file_path: "internal/spec/active_feature.go"
          dependencies: []
          acceptance_criteria:
            - "Define focused exported or package-level types for selection source, resolution input, and resolution result."
            - "Keep the file compile-safe without implementing precedence behavior yet."
            - "Use names that make explicit, persisted, git_branch, and fallback selection sources obvious."
        - id: "T002"
          title: "Identify existing spec lookup entry points in internal/spec and internal/cli"
          status: "Completed"
          type: "setup"
          parallel: false
          story_id: null
          file_path: "internal/spec/active_feature.go"
          dependencies: []
          acceptance_criteria:
            - "Record the existing helpers that perform branch-prefix, recent-directory, positional argument, and --spec lookup."
            - "Add short implementation notes near the resolver skeleton only if they prevent duplicated command-level logic."
            - "Do not change command behavior in this setup task."
    - number: 2
      title: "Foundational"
      purpose: "Build project-local active feature state and shared resolver behavior used by all stories."
      tasks:
        - id: "T003"
          title: "Add resolver precedence tests in internal/spec/active_feature_test.go"
          status: "Completed"
          type: "test"
          parallel: false
          story_id: null
          file_path: "internal/spec/active_feature_test.go"
          dependencies: ["T001", "T002"]
          acceptance_criteria:
            - "Use map[string]struct table tests for explicit selection, persisted selection, branch fallback, and no-match errors."
            - "Assert precedence order is explicit selection, persisted state, then existing branch or fallback lookup."
            - "Assert the resolver returns the selected feature directory and a selection source."
        - id: "T004"
          title: "Add persisted state tests in internal/spec/active_feature_state_test.go"
          status: "Completed"
          type: "test"
          parallel: true
          story_id: null
          file_path: "internal/spec/active_feature_state_test.go"
          dependencies: ["T001", "T002"]
          acceptance_criteria:
            - "Use map[string]struct table tests for load, save, missing state, deleted directory, missing spec.yaml, and path traversal rejection."
            - "Verify the YAML state includes feature_directory, source, and updated_at."
            - "Verify invalid persisted values produce contextual, actionable errors."
        - id: "T005"
          title: "Implement project-local persisted active feature state in internal/spec/active_feature_state.go"
          status: "Completed"
          type: "implementation"
          parallel: false
          story_id: null
          file_path: "internal/spec/active_feature_state.go"
          dependencies: ["T004"]
          acceptance_criteria:
            - "Store state under .autospec/state/active-feature.yaml relative to the project root."
            - "Load, save, clear, and validate state with contextual error wrapping."
            - "Reject persisted paths that escape the configured specs directory."
        - id: "T006"
          title: "Implement shared active feature resolver in internal/spec/active_feature.go"
          status: "Completed"
          type: "implementation"
          parallel: false
          story_id: null
          file_path: "internal/spec/active_feature.go"
          dependencies: ["T003", "T005"]
          acceptance_criteria:
            - "Resolve explicit command selection before persisted state and branch-derived fallback."
            - "Reuse existing metadata and spec directory helpers instead of duplicating directory scanning logic."
            - "Return deterministic selection source details for status and error output."
        - id: "T007"
          title: "Add CLI-level resolver fixture helpers in internal/cli/active_feature_test.go"
          status: "Completed"
          type: "test"
          parallel: true
          story_id: null
          file_path: "internal/cli/active_feature_test.go"
          dependencies: ["T003", "T004"]
          acceptance_criteria:
            - "Provide reusable helpers for creating specs, persisted active state, and branch-mismatch command contexts."
            - "Keep helper functions short and scoped to CLI tests."
            - "Do not require real agent execution or external services."
    - number: 3
      title: "User Story 1 - Use persisted active feature directory"
      purpose: "Allow autospec workflow commands to continue against the persisted feature directory when the current branch name differs."
      story_reference: "US-001"
      independent_test: "Create or select a feature directory, switch to a differently named branch, and verify autospec still resolves the selected spec."
      tasks:
        - id: "T008"
          title: "Add persisted selection workflow tests in internal/cli/stages/stages_test.go"
          status: "Completed"
          type: "test"
          parallel: false
          story_id: "US-001"
          file_path: "internal/cli/stages/stages_test.go"
          dependencies: ["T006", "T007"]
          acceptance_criteria:
            - "Use map[string]struct tests covering plan, tasks, and implement resolution from persisted state on a mismatched branch."
            - "Assert lifecycle/history command wrappers still execute for stage commands."
            - "Assert missing persisted artifacts produce clear validation errors."
        - id: "T009"
          title: "Persist feature state after feature creation or selection in internal/cli/run.go"
          status: "Completed"
          type: "implementation"
          parallel: false
          story_id: "US-001"
          file_path: "internal/cli/run.go"
          dependencies: ["T008"]
          acceptance_criteria:
            - "Persist the feature directory after autospec creates a new feature during run/specify flows."
            - "Persist only deliberate active selections, not every one-shot override unless existing behavior implies it."
            - "Wrap persistence errors with command context and avoid corrupting generated spec artifacts."
        - id: "T010"
          title: "Route stage commands through active feature resolver in internal/cli/stages"
          status: "Completed"
          type: "implementation"
          parallel: false
          story_id: "US-001"
          file_path: "internal/cli/stages/plan.go"
          dependencies: ["T008", "T006"]
          acceptance_criteria:
            - "Update plan, tasks, and implement stage commands to use the shared resolver when no explicit --spec value is provided."
            - "Preserve lifecycle.RunWithHistory or lifecycle.RunWithHistoryContext wrappers."
            - "Keep branch-prefix fallback behavior unchanged when no persisted state exists."
        - id: "T011"
          title: "Route status command through active feature resolver in internal/cli/util/status.go"
          status: "Completed"
          type: "implementation"
          parallel: true
          story_id: "US-001"
          file_path: "internal/cli/util/status.go"
          dependencies: ["T006", "T007"]
          acceptance_criteria:
            - "Resolve status with persisted active feature state when no positional feature is supplied."
            - "Display the resolved feature directory and selection source in status output."
            - "Return actionable errors for deleted persisted directories or missing spec.yaml."
        - id: "T012"
          title: "Add branch-mismatch status tests in internal/cli/util/status_test.go"
          status: "Completed"
          type: "test"
          parallel: true
          story_id: "US-001"
          file_path: "internal/cli/util/status_test.go"
          dependencies: ["T006", "T007"]
          acceptance_criteria:
            - "Use map[string]struct tests for persisted state on mismatched branches and stale persisted state."
            - "Assert output identifies both the active feature directory and selection source."
            - "Assert existing no-feature error behavior remains clear."
    - number: 4
      title: "User Story 2 - Override active feature explicitly"
      purpose: "Ensure scripts and agents can choose a feature for one invocation and receive validation errors when the explicit target is invalid."
      story_reference: "US-002"
      independent_test: "Set an explicit feature directory while another feature is persisted and verify the explicit selection wins."
      tasks:
        - id: "T013"
          title: "Add explicit override precedence tests in internal/cli/run_test.go"
          status: "Completed"
          type: "test"
          parallel: false
          story_id: "US-002"
          file_path: "internal/cli/run_test.go"
          dependencies: ["T006", "T007"]
          acceptance_criteria:
            - "Use map[string]struct tests where --spec points to one feature and persisted state points to another."
            - "Assert --spec wins and reports explicit selection."
            - "Assert invalid explicit selections fail without falling back to persisted state."
        - id: "T014"
          title: "Apply explicit selection precedence in internal/cli/run.go"
          status: "Completed"
          type: "implementation"
          parallel: false
          story_id: "US-002"
          file_path: "internal/cli/run.go"
          dependencies: ["T013", "T009"]
          acceptance_criteria:
            - "Pass --spec values into the shared active feature resolver as explicit selections."
            - "Do not let persisted state override explicit command input."
            - "Keep existing run stage ordering and confirmation behavior intact."
        - id: "T015"
          title: "Add artifact explicit-selection tests in internal/cli/artifact_test.go"
          status: "Completed"
          type: "test"
          parallel: true
          story_id: "US-002"
          file_path: "internal/cli/artifact_test.go"
          dependencies: ["T006", "T007"]
          acceptance_criteria:
            - "Cover type-only artifact lookup with persisted state and explicit file path lookup."
            - "Assert invalid explicit feature selection produces exit-code-appropriate validation output."
            - "Avoid invoking real workflow agents."
        - id: "T016"
          title: "Route artifact type-only lookup through resolver in internal/cli/artifact.go"
          status: "Completed"
          type: "implementation"
          parallel: false
          story_id: "US-002"
          file_path: "internal/cli/artifact.go"
          dependencies: ["T015"]
          acceptance_criteria:
            - "Use persisted active feature state when artifact lookup receives an artifact type instead of a file path."
            - "Preserve direct file path validation behavior."
            - "Report selected directory and validation problem when the resolved feature is incomplete."
    - number: 5
      title: "User Story 3 - Preserve existing spec selection behavior"
      purpose: "Keep branch-prefix and --spec workflows working for current users while adding persisted state support."
      story_reference: "US-003"
      independent_test: "Run existing status and workflow commands with branch-prefix selection and --spec override after adding persisted state support."
      tasks:
        - id: "T017"
          title: "Add backward compatibility tests in internal/spec/metadata_test.go"
          status: "Pending"
          type: "test"
          parallel: false
          story_id: "US-003"
          file_path: "internal/spec/metadata_test.go"
          dependencies: ["T006"]
          acceptance_criteria:
            - "Use map[string]struct tests to verify branch-prefix lookup works when no persisted state exists."
            - "Cover multiple feature directories sharing a numeric prefix where existing fallback remains deterministic."
            - "Assert existing --spec-compatible identifiers still resolve through current metadata helpers."
        - id: "T018"
          title: "Preserve branch fallback integration in internal/spec/active_feature.go"
          status: "Pending"
          type: "implementation"
          parallel: false
          story_id: "US-003"
          file_path: "internal/spec/active_feature.go"
          dependencies: ["T017", "T006"]
          acceptance_criteria:
            - "Keep existing branch-derived lookup as the resolver fallback when explicit and persisted selection are absent."
            - "Do not alter spec.yaml, plan.yaml, or tasks.yaml schemas."
            - "Return contextual errors that identify whether failure came from explicit, persisted, branch, or fallback lookup."
        - id: "T019"
          title: "Add workflow compatibility regression tests in internal/cli/prep_test.go"
          status: "Pending"
          type: "test"
          parallel: false
          story_id: "US-003"
          file_path: "internal/cli/prep_test.go"
          dependencies: ["T014", "T018"]
          acceptance_criteria:
            - "Verify prep keeps resolving by branch prefix when no active feature state file exists."
            - "Verify --spec overrides a different persisted active feature."
            - "Keep tests isolated from real user config and global autospec state."
        - id: "T020"
          title: "Update prep and all command resolution in internal/cli/prep.go and internal/cli/all.go"
          status: "Pending"
          type: "implementation"
          parallel: false
          story_id: "US-003"
          file_path: "internal/cli/prep.go"
          dependencies: ["T019"]
          acceptance_criteria:
            - "Use the shared resolver for prep and all flows without removing current lifecycle/history wrappers."
            - "Preserve --spec override behavior as higher priority than persisted state."
            - "Preserve branch-prefix fallback for users with no persisted state."
    - number: 6
      title: "Polish & Cross-Cutting Concerns"
      purpose: "Document behavior, update release notes, provide manual validation guidance, and run required project checks."
      tasks:
        - id: "T021"
          title: "Document active feature resolution in docs/public/worktree.md"
          status: "Pending"
          type: "documentation"
          parallel: true
          story_id: null
          file_path: "docs/public/worktree.md"
          dependencies: ["T010", "T014", "T018", "T020"]
          acceptance_criteria:
            - "Explain resolution order: explicit selection, persisted active feature, branch-prefix fallback."
            - "Describe multi-worktree usage and state that Git still prevents checking out the same branch in multiple worktrees."
            - "Include recovery guidance for stale or invalid persisted state."
        - id: "T022"
          title: "Update command reference in docs/public/reference.md"
          status: "Pending"
          type: "documentation"
          parallel: true
          story_id: null
          file_path: "docs/public/reference.md"
          dependencies: ["T010", "T014", "T016", "T020"]
          acceptance_criteria:
            - "Document how status, run, prep, stage commands, and artifact lookup choose the active feature."
            - "Clarify --spec precedence over persisted state."
            - "Avoid documenting unsupported same-branch multi-worktree behavior."
        - id: "T023"
          title: "Create manual testing plan in .dev/tasks/128-persist-feature-directory.md"
          status: "Pending"
          type: "documentation"
          parallel: true
          story_id: null
          file_path: ".dev/tasks/128-persist-feature-directory.md"
          dependencies: ["T010", "T014", "T016", "T018", "T020"]
          acceptance_criteria:
            - "Include manual scenarios for persisted branch mismatch, explicit override, stale state, branch fallback, and artifact lookup."
            - "Include a Report Summaries section for later results."
            - "Do not execute the manual tests as part of this task."
        - id: "T024"
          title: "Update changelog and run full validation"
          status: "Pending"
          type: "documentation"
          parallel: false
          story_id: null
          file_path: "internal/changelog/changelog.yaml"
          dependencies: ["T021", "T022", "T023"]
          acceptance_criteria:
            - "Add 1-3 user-facing bullets to internal/changelog/changelog.yaml and run make changelog-sync."
            - "Run make fmt, make test, make lint, and make build; all must exit 0."
            - "Fix regressions or document any blocker with the failing command and relevant output."
dependencies:
    user_story_order:
        - story_id: "US-001"
          depends_on: []
          blocks: ["US-002", "US-003"]
        - story_id: "US-002"
          depends_on: ["US-001"]
          blocks: ["US-003"]
        - story_id: "US-003"
          depends_on: ["US-001", "US-002"]
          blocks: []
    phase_order:
        - phase: 1
          blocks: [2]
        - phase: 2
          blocks: [3, 4, 5]
        - phase: 3
          blocks: [4, 5, 6]
        - phase: 4
          blocks: [5, 6]
        - phase: 5
          blocks: [6]
        - phase: 6
          blocks: []
parallel_execution:
    - phase: 2
      parallel_groups:
        - tasks: ["T004", "T007"]
          rationale: "State tests and CLI fixture helpers touch different packages after the resolver skeleton exists."
    - phase: 3
      parallel_groups:
        - tasks: ["T011", "T012"]
          rationale: "Status command implementation and status-specific tests can be developed alongside stage command wiring once resolver behavior exists."
    - phase: 4
      parallel_groups:
        - tasks: ["T013", "T015"]
          rationale: "Run command override tests and artifact command tests cover separate files and can be written together after resolver fixtures exist."
    - phase: 6
      parallel_groups:
        - tasks: ["T021", "T022", "T023"]
          rationale: "Worktree docs, reference docs, and manual testing plan touch separate documents and depend on completed command behavior."
implementation_strategy:
    mvp_scope:
        phases: [1, 2, 3]
        description: "Setup + shared resolver/state infrastructure + persisted active feature use in primary stage and status commands."
        validation: "Run resolver/state unit tests plus persisted branch-mismatch stage/status tests; manually inspect status output for selection source."
    incremental_delivery:
        - milestone: "Resolver Ready"
          phases: [1, 2]
          deliverable: "A tested internal resolver that can load persisted active feature state and preserve branch fallback."
        - milestone: "MVP Complete"
          phases: [1, 2, 3]
          deliverable: "Workflow stages and status can use persisted active feature directories across branch name mismatches."
        - milestone: "Explicit Override Complete"
          phases: [1, 2, 3, 4]
          deliverable: "--spec and explicit artifact selection win over persisted state with clear validation errors."
        - milestone: "Compatibility Complete"
          phases: [1, 2, 3, 4, 5]
          deliverable: "Existing branch-prefix, prep, all, and --spec workflows remain covered by regression tests."
        - milestone: "Release Ready"
          phases: [1, 2, 3, 4, 5, 6]
          deliverable: "Docs, manual test plan, changelog, and full make validation are complete."
_meta:
    version: "1.0.0"
    generator: "autospec"
    generator_version: "autospec dev"
    created: "2026-05-14T01:45:14Z"
    artifact_type: "tasks"
